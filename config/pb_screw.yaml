# pb_screw - Threaded screw/bolt
# Dimensions: ~29 x 25 x 70 mm (width x depth x height)
# Grasp strategy: Top-down, grip across the ~25mm shaft

object_name: "pb_screw"
object_diameter_m: 0.025

# Pose topic
input_pose_topic: "/perception/fp/pose_base/zed2i_2/pb_screw_0"

# Gripper settings - "always close" strategy
gripper_grasp_width: 0.001
gripper_grasp_force: 25.0
gripper_grasp_speed: 0.03
gripper_epsilon_inner: 0.001
gripper_epsilon_outer: 0.08

# Centroid offset
centroid_offset_x: 0.0
centroid_offset_y: 0.0
centroid_offset_z: 0.0

# Clearances
pregrasp_clearance_m: 0.10
grasp_clearance_m: 0.020

# End-effector orientation (top-down)
roll: 3.14159265
pitch: 0.0
yaw: 1.57079633

# Lift height
lift_delta_z_m: 0.08
# cooling_screw - Small cylindrical screw
# Dimensions: ~20 x 20 x 35 mm (diameter x diameter x height)
# Grasp strategy: Top-down, grip across the 20mm diameter

object_name: "cooling_screw"
object_diameter_m: 0.020

# Pose topic
input_pose_topic: "/perception/fp/pose_base/zed2i_2/cooling_screw_1"

# Gripper settings - "always close" strategy
gripper_grasp_width: 0.001
gripper_grasp_force: 25.0
gripper_grasp_speed: 0.03
gripper_epsilon_inner: 0.001
gripper_epsilon_outer: 0.08

# Centroid offset
centroid_offset_x: 0.0
centroid_offset_y: 0.0
centroid_offset_z: 0.0

# Clearances
pregrasp_clearance_m: 0.08
grasp_clearance_m: 0.01

# End-effector orientation (top-down)
roll: 3.14159265
pitch: 0.0
yaw: 1.57079633

# Lift height
lift_delta_z_m: 0.06